# Host Nginx entrypoint for tools.heersin.cloud
# This file proxies public traffic to the Dockerized web gateway at 127.0.0.1:4200.

server {
    listen 80;
    server_name tools.heersin.cloud;
    client_max_body_size 16m;

    location / {
        proxy_pass http://127.0.0.1:4200;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# Optional TLS block (enable after certificates are ready):
# server {
#     listen 443 ssl http2;
#     server_name tools.heersin.cloud;
#
#     ssl_certificate /etc/letsencrypt/live/tools.heersin.cloud/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/tools.heersin.cloud/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:4200;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
# }
