{
  "name": "tools-subsite",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/tools-web"
  ],
  "scripts": {
    "generate:catalog": "node scripts/build-tool-catalog.mjs",
    "build:wasm": "./scripts/build-wasm.sh",
    "dev:web": "npm run --workspace apps/tools-web dev",
    "build:web": "npm run --workspace apps/tools-web build",
    "lint:web": "npm run --workspace apps/tools-web lint"
  },
  "devDependencies": {
    "yaml": "^2.8.1"
  }
}
